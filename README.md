# File-Sorter
FileSorter is a Python script that organizes files based on their extensions. It reads filenames and extensions from an Excel sheet, creates missing files, and sorts them into folders by type. Perfect for automating file organization tasks!
